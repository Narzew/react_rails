default: &default
  adapter: mysql2
  encoding: utf8
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: root
  password: ""

development:
  <<: *default
  database: Training_db_dev

test:
  <<: *default
  database: Training_db_test

production:
  <<: *default
  database: Training_db_prod
